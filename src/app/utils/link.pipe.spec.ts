/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { LinkPipe } from './link.pipe';

describe('Pipe: Linke', () => {
  it('create an instance', () => {
    let pipe = new LinkPipe();
    expect(pipe).toBeTruthy();
  });
});